{
  "name": "ecs-on-fargate",
  "private": true,
  "engines": {
    "pnpm": "*",
    "npm": "Use pnpm instead"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "app": "pnpm -F \"app\"",
    "cdk": "pnpm -F \"cdk\""
  },
  "devDependencies": {
    "@types/node": "20.4.2",
    "prettier": "3.0.0",
    "typescript": "5.1.6"
  }
}
